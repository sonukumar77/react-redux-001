// import { useSelector } from "react-redux";

const Home = () => {

    // const isLight  = useSelector((state) => state.isLight)
    return(
        <>
        <h1>Home </h1>
        </>
    )
}

export default Home;